apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-iam
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Name }}-iam
  ports:
    - port: {{ .Values.iam.service.port }}
      targetPort: {{ .Values.iam.service.port }}
  type: ClusterIP